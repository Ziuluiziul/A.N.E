A.N.E. credential-input leak reproductions
All values were synthetic. No configured secret file was read or written.

Reproduction A — FastAPI/Pydantic validation boundary

  candidate = "Z" * 513
  client.put("/api/control/providers/google/credential", json={"key": candidate})

Observed summary (the response itself is intentionally not stored):

  oversize_status 422
  candidate_full_in_response True
  error_type string_too_long
  input_field_present True
  input_length 513

Reproduction B — adapter exception boundary

  Settings(_env_file=None), no configured keys
  fake adapter raises ProviderAuthError("provider echoed " + candidate)
  client.post("/api/control/providers/google/test", json={"key": candidate})

Observed summary (candidate omitted):

  adapter_status 200
  returned_status invalido
  candidate_prefix_in_detail True
  detail_length 300

Required invariant:

  No candidate value, including rejected input, may occur in a response, log,
  traceback or persisted event. A safe test may compare in memory and report only a
  boolean/length, as this evidence does.
