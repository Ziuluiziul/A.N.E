# Handoff — 2026-08-02

Estado reconciliado a partir do qual os próximos agentes trabalham, e os dois prompts
prontos para colar. O código do fechamento culmina em `c2e36db`; `560f49a`, seu filho
direto, acrescenta apenas a primeira versão deste handoff. O commit documental que
contém esta reconciliação é o sucessor imediato de `560f49a` e altera somente este
arquivo e `docs/CICLO-1.1-FECHAMENTO-ATLAS-2026-08-02.md`.

O SHA desse commit documental não é gravado dentro dele: um commit não pode conter o
próprio identificador sem que a gravação mude o identificador. Para reconhecê-lo sem
ambiguidade, confira o HEAD real, o pai `560f49a` e o assunto
`Reconcilia o fechamento antes do Ciclo 2`.

## Estado factual

```text
repositório     /home/ziul/Projetos/vault-autodidata
HEAD de entrada 560f49aa725aad0246075e87df41d2f6d382ed3f
HEAD de código  c2e36dbd42de3bb0bba65f0d3aeb5d8b6cdf793d
HEAD documental commit que contém esta reconciliação; pai 560f49a
tag             baseline-pos-migracao-2026-07-30 → 7aa5db1
working tree    limpa no início do Gate 0
corpus          81 notas · 627 wikilinks · 267 claims · 12 MOCs · 11 domínios
manifesto       4f5b1d009041583c89957f7c22199d8d77836f4441f7e6aa16e741da8b2bf5bb
testes          138 pytest · 53 vitest · ruff, mypy, tsc, eslint limpos
bundle entrada  vault-autodidata-ciclo-1.1-fechamento-560f49a.bundle
                3949cfa0d6876fc7f02bde05b8081f31a705daabc3dea9da6429ca1b0e3b5286
tarball entrada vault-autodidata-ciclo-1.1-fechamento-560f49a.tar.gz
                0cfddf9391c54ada90c7a1eb7623fa18adb52f601a65b8945f78f860de6a3b8a
```

O bundle acima foi aprovado por `git bundle verify`, com `HEAD` e `main` em
`560f49a`; o tarball passou por `gzip -t` e `tar -tzf`, e
`git get-tar-commit-id` devolveu o mesmo commit. Os nomes e hashes antes publicados
para `c2e36db` não correspondem a nenhum arquivo retido nos backups e ficam
formalmente substituídos por este par verificado.

Bundle e tarball do commit documental são produzidos depois do commit. Seus nomes
levam o HEAD e os SHA-256 ficam no arquivo `.sha256` adjacente, em
`~/Documentos/Backups/Vault/2026-08-02/`. Essa fonte externa evita a circularidade de
tentar gravar, dentro de um commit, o hash de um artefato gerado a partir dele.

## Cadeia reconciliada

```text
6d267ab → 81477b5 → c109132 → 89161ea → 2c7a0ec → b9d5752
         → c2e36db → 560f49a → commit documental desta reconciliação
```

| Commit | Pai | Assunto | Arquivos alterados |
| --- | --- | --- | --- |
| `6d267ab` | `1be744e` | Registra o relatório do Ciclo 1 | adiciona `docs/CICLO-1-ATLAS-FULL-3D-2026-08-02.md` |
| `2c7a0ec` | `89161ea` | Produz evidência visual a partir da própria aplicação | modifica quatro arquivos de configuração do frontend |
| `c2e36db` | `b9d5752` | Dá profundidade real ao atlas, sem soltá-lo | modifica o relatório, `layout.ts` e dois arquivos de teste |
| `560f49a` | `c2e36db` | Deixa o handoff com os prompts dos próximos agentes | adiciona `docs/HANDOFF-2026-08-02.md` |

`c2e36db` introduziu a calota dos MOCs, os territórios volumétricos, o relaxamento
tridimensional e os gates de profundidade. `560f49a` não alterou código.

Ler antes de qualquer coisa: `AGENTS.md`, `docs/CICLO-1-ATLAS-FULL-3D-2026-08-02.md`,
`docs/CICLO-1.1-FECHAMENTO-ATLAS-2026-08-02.md`, `docs/ADR-001-paleta-oklch.md`.

Pendências reais registradas no relatório do Ciclo 1.1: sem watcher de corpus, sem
produtor para a camada operacional (o Writer Gateway não existe), smoke tests dos
provedores nunca executados apesar de as três chaves estarem presentes, e o OAuth do
Workspace sem `client_secret`.

---

## Prompt A — para o ChatGPT no navegador (revisão e arquitetura)

```text
Você está revisando o projeto vault-autodidata, um ambiente autodidata
interdisciplinar sobre um corpus acadêmico de 81 notas. Não tem acesso à máquina:
seu papel é criticar decisões e propor arquitetura, não alegar execução.

Estado de código: commit c2e36db, seguido por 560f49a e por um commit exclusivamente
documental de reconciliação. São 138 testes Python e 53 TypeScript verdes; o
manifesto do corpus 4f5b1d00… permanece inalterado desde a baseline.

O que já existe e NÃO deve ser refeito:

- corpus somente leitura em knowledge/, com auditor estrutural próprio;
- identidade de nota por caminho relativo NFC; wikilink ambíguo reprova a projeção
  em vez de escolher um candidato;
- contrato de projeção versionado (v1.0.0), sanitizado, sem caminho absoluto, com
  impressão digital igual ao manifesto do auditor;
- Atlas 3D em Three.js puro: âncoras de MOC numa calota, territórios volumétricos,
  geometria por tipo de entidade, arestas tipadas por padrão e marcador, texto SDF,
  LOD por pixel projetado com histerese, ilha de controles 3D, modo textual de
  fallback sem WebGL;
- memória espacial persistida fora do corpus, com schema versionado e recuperação
  de corrupção;
- camada operacional implementada e vazia em produção, com fixture só sob flag;
- três adaptadores de provedor sem retry intrarrun, com SecretStr e redaction.

O que eu preciso de você, nesta ordem:

1. Critique a decisão de amarrar a amplitude da calota ao raio quantizado das
   âncoras em vez de à extensão total da cena. Ela troca fidelidade literal ao
   dossiê por estabilidade do mapa mental. O parâmetro controlador é 0,22 do raio
   estrutural; a razão contra a extensão horizontal da cena é só diagnóstica e pode
   ficar em 0,13–0,30, portanto não há conformidade literal universal com 0,15–0,25.
   Um envelope estrutural versionado seria uma terceira opção melhor?

2. Proponha o desenho do Writer Gateway — o único componente autorizado a escrever
   no corpus. Precisa produzir eventos canônicos de procedência, aceitar patch
   validado, e nunca permitir que um agente escreva direto. Quero fronteiras,
   estados e o formato do evento, não código.

3. Proponha como o watcher deve reconciliar edições externas do corpus enquanto o
   Atlas está aberto, sem invalidar a memória espacial a cada salvamento de arquivo.

4. Aponte o que, na governança epistêmica atual, ainda depende de disciplina humana
   e poderia virar invariante verificável sem criar burocracia nova.

Restrições que valem para qualquer proposta: nada de LangChain, CrewAI, AutoGen,
Docker, banco externo ou framework de agentes que esconda o fluxo; nada de escrita
automática no corpus; nada de fabricar procedência; e o rigor máximo pertence ao
conteúdo acadêmico, não à mecânica operacional.

Responda com decisões e trade-offs, curto e específico. Se discordar de algo que
já está implementado, diga o que exatamente reprova e sob qual critério.
```

---

## Prompt B — para o Codex / GPT-5.6 SOL no VS Code (execução local)

```text
Você tem acesso à máquina e trabalha no repositório /home/ziul/Projetos/vault-autodidata.

Antes de alterar qualquer coisa: confirme HEAD, branch e working tree; rode
`make audit` e confirme o manifesto 4f5b1d00…; leia AGENTS.md,
docs/CICLO-1.1-FECHAMENTO-ATLAS-2026-08-02.md e docs/ADR-001-paleta-oklch.md.
O histórico esperado contém, nesta ordem, c2e36db (última alteração funcional),
560f49a (handoff original) e um sucessor exclusivamente documental com pai 560f49a
e assunto "Reconcilia o fechamento antes do Ciclo 2". Descubra o SHA real com Git;
ele não é hardcoded no próprio commit. Se essa cadeia não estiver na ancestralidade
do HEAD, ou se houver alterações posteriores, descubra o que mudou antes de continuar.

Regras inegociáveis:

- knowledge/ é somente leitura. Nenhum Markdown canônico é alterado, nem para
  facilitar parser, teste ou demonstração.
- Proibidos: git reset --hard, git clean, git checkout -- ., rebase de commits
  existentes, mover a tag baseline-pos-migracao-2026-07-30.
- Nenhum valor de credencial aparece em log, relatório, teste ou resposta. Só
  presença, ausência, permissão e caminho.
- Nenhuma chamada externa a provedor sem que a tarefa peça, e no máximo uma
  requisição por execução, sem retry dentro da mesma execução.
- Encerre servidores que você subir pelos PIDs próprios, nunca com pkill genérico.

Tarefas deste ciclo, em ordem, sem ampliar escopo:

1. WATCHER DE CORPUS. `watchfiles` já é dependência. Recalcular a projeção quando
   knowledge/ muda, sem invalidar a memória espacial a cada salvamento: a impressão
   digital só muda quando o conteúdo muda de fato, então a chave do layout deve
   sobreviver a salvamentos que não alteram bytes. Entidades novas entram por
   colocação local; MOCs existentes não se movem. Testes obrigatórios: arquivo
   tocado sem mudança de conteúdo não muda a impressão; nota nova preserva as
   posições anteriores; nota removida some do layout.

2. SMOKE TESTS DOS PROVEDORES. As três chaves estão presentes em
   ~/.config/vault-autodidata/secrets.env. Rode `make discover-models` e
   `make smoke-providers` uma vez cada. Registre por provedor: endpoints listados,
   resultado da sonda, headers de limite observados e latência. Não teste todos os
   modelos, não faça benchmark, não envie conteúdo do corpus nos prompts. Se algum
   provedor responder 429, registre o retry-after e pare — não repita.

3. OAUTH DO WORKSPACE, se e somente se o client_secret existir. Caso não exista,
   apenas confirme que `make workspace-oauth` falha com mensagem curta e sem
   traceback, e siga.

4. RELATÓRIO em docs/CICLO-2-<assunto>-<data>.md, com estado inicial real,
   divergências em relação a este handoff, o que foi feito, testes, hashes,
   pendências e comandos de reprodução.

Verificação antes de encerrar: `make audit`, `make test`, `make lint`, build do
frontend, working tree limpa, e bundle mais tarball por `git archive HEAD` em
~/Documentos/Backups/Vault/<data>/ com SHA-256 registrado.

Comece a resposta por STATUS: PASS, STATUS: PARTIAL ou STATUS: BLOCKED. Não declare
PASS por aproximação — se um item ficou de fora, é PARTIAL, e diga qual.
```
